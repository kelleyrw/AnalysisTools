#ifndef LT_IS_ZERO_H
#define LT_IS_ZERO_H

namespace lt
{
    bool is_zero(const int value);
    bool is_zero(const long int value);
    bool is_zero(const float value);
    bool is_zero(const double value);
    bool is_zero(const long double value);

} // namespace lt

#endif // LT_IS_ZERO_H
